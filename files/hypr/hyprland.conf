# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hypr.land/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hypr.land/Configuring/Monitors/
# Dynamique : n'importe quel écran, résolution auto, mais ÉCHELLE forcée à 1 (pas de zoom)
monitor=,preferred,auto,1
# Réglage spécifique laptop (décommente pour forcer) :
# monitor=eDP-1,1920x1080@60,0x0,1
xwayland {
    force_zero_scaling = true
}



###################
### MY PROGRAMS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = thunar
$menu = rofi -show drun


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = waybar
exec-once = nm-applet
exec-once = swayosd-server   # daemon SwayOSD (mic mute, caps/num lock, player-menu)
exec-once = python ~/.config/hypr/scripts/bar-osd-daemon.py   # OSD « barres FNAF » (volume + luminosité)
exec-once = hyprpaper   # fond statique de base (visible sur batterie quand mpvpaper est coupé)
# Fond d'écran animé : décodage GPU + auto-pause + coupé sur batterie (voir le script)
exec-once = ~/.config/hypr/scripts/mpv-wallpaper.sh
# Opacité des terminaux kitty selon le workspace (opaque dans "magic", 0.5 ailleurs)
exec-once = ~/.config/hypr/scripts/kitty-magic-opacity.py

#exec-once = ~/.config/hypr/scripts/mpv-wall.sh
#exec-once = ~/.config/hypr/scripts/hypr-spiderman-wall.sh

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hypr.land/Configuring/Environment-variables/

env = XCURSOR_THEME,Vimix-white-cursors
env = XCURSOR_SIZE,24
env = LIBVA_DRIVER_NAME,nvidia
env = __GLX_VENDOR_LIBRARY_NAME,radeonsi

###################
### PERMISSIONS ###
###################

# See https://wiki.hypr.land/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hypr.land/Configuring/Variables/

# https://wiki.hypr.land/Configuring/Variables/#general
general {
    gaps_in = 5
    gaps_out = 20

    border_size = 0

    # https://wiki.hypr.land/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hypr.land/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hypr.land/Configuring/Variables/#decorationa
decoration {
    rounding = 8
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0



    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hypr.land/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 7
        passes = 2
	new_optimizations = on
    }
}
layerrule = match:namespace ^(waybar)$, blur off

# Rofi : apparition « waow » — zoom-in spectaculaire depuis un point (popin 1%)
# avec décélération easeOutQuint. Identique pour le lanceur ($mod+Return), le
# powermenu de la waybar et le player-menu (même namespace layer-shell « rofi »).
layerrule = match:namespace ^(rofi)$, animation popin 1%

# https://wiki.hypr.land/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default curves, see https://wiki.hypr.land/Configuring/Animations/#curves
    #        NAME,           X0,   Y0,   X1,   Y1
    bezier = easeOutQuint,   0.23, 1,    0.32, 1
    bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
    bezier = linear,         0,    0,    1,    1
    bezier = almostLinear,   0.5,  0.5,  0.75, 1
    bezier = quick,          0.15, 0,    0.1,  1
    bezier = smooth,         0.45, 0,    0.15, 1

    # Default animations, see https://wiki.hypr.land/Configuring/Animations/
    #           NAME,          ONOFF, SPEED, CURVE,        [STYLE]
    animation = global,        1,     10,    default
    animation = border,        1,     5.39,  easeOutQuint
    animation = windows,       1,     4.79,  easeOutQuint
    animation = windowsIn,     1,     4.1,   easeOutQuint, popin 87%
    animation = windowsOut,    1,     1.49,  linear,       popin 87%
    animation = fadeIn,        1,     1.73,  almostLinear
    animation = fadeOut,       1,     1.46,  almostLinear
    animation = fade,          1,     3.03,  quick
    animation = layers,        1,     3.81,  easeOutQuint
    animation = layersIn,      1,     4,     easeOutQuint, fade
    animation = layersOut,     1,     1.5,   linear,       fade
    animation = fadeLayersIn,  1,     1.79,  almostLinear
    animation = fadeLayersOut, 1,     1.39,  almostLinear
    animation = workspaces,       1,  2.3,   smooth, slide
    animation = workspacesIn,     1,  2.3,   smooth, slide
    animation = workspacesOut,    1,  2.3,   smooth, slide
    animation = specialWorkspace, 1,  2.3,   smooth, slidevert
    animation = zoomFactor,       1,  7,     quick
}

# Ref https://wiki.hypr.land/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hypr.land/Configuring/Dwindle-Layout/ for more
dwindle {
    preserve_split = true # You probably want this
}

# See https://wiki.hypr.land/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hypr.land/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hypr.land/Configuring/Variables/#input
input {
    kb_layout = fr
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

# See https://wiki.hypr.land/Configuring/Gestures
gesture = 3, horizontal, workspace

# Example per-device config
# See https://wiki.hypr.land/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hypr.land/Configuring/Binds/ for more
bind = $mainMod, Q, exec, $terminal
bind = $mainMod, B, killactive,
bind = $mainMod, M, exec, bash -c 'choice=$(printf "Non\nOui" | rofi -dmenu -i -p "Quitter Hyprland ?"); case "${choice,,}" in oui|o|yes|y) hyprctl dispatch exit ;; esac'
bind = $mainMod, F, fullscreenstate, 3
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, T, togglefloating,
bind = $mainMod, R, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, S, layoutmsg, togglesplit
bind = , F11, exec, hyprlock

bind = ALT, F, exec, firefox
bind = ALT, D, exec, discord

# Move focus with mainMod + arrow keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

bind = ALT, h, movewindoworgroup, l
bind = ALT, l, movewindoworgroup, r
bind = ALT, k, movewindoworgroup, u
bind = ALT, j, movewindoworgroup, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, code:10, workspace, 1
bind = $mainMod, code:11, workspace, 2
bind = $mainMod, code:12, workspace, 3
bind = $mainMod, A, workspace, 4
bind = $mainMod, Z, workspace, 5

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = ALT, code:10, movetoworkspace, 1
bind = ALT, code:11, movetoworkspace, 2
bind = ALT, code:12, movetoworkspace, 3
bind = ALT, A, movetoworkspace, 4
bind = ALT, Z, movetoworkspace, 5

# Example special workspace (scratchpad)
bind = $mainMod, V, togglespecialworkspace, magic
bind = ALT, V, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Screenshot
bind = , Print, exec, sh -c 'grim -g "$(slurp -d)" - | wl-copy'

# Color
bind = $mainMod, C, exec, hyprpicker -a

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod SHIFT, mouse:272, resizewindow

# Laptop multimedia keys for volume and LCD brightness
# Volume / luminosité -> OSD « barres FNAF » maison (bar-osd.sh)
bindel = ,XF86AudioRaiseVolume,  exec, ~/.config/hypr/scripts/bar-osd.sh volume raise
bindel = ,XF86AudioLowerVolume,  exec, ~/.config/hypr/scripts/bar-osd.sh volume lower
bindel = ,XF86AudioMute,         exec, ~/.config/hypr/scripts/bar-osd.sh volume mute
bindel = ,XF86MonBrightnessUp,   exec, ~/.config/hypr/scripts/bar-osd.sh brightness raise
bindel = ,XF86MonBrightnessDown, exec, ~/.config/hypr/scripts/bar-osd.sh brightness lower
# Mic mute conservé sur SwayOSD (simple bascule, pas de barre de niveau)
bindel = ,XF86AudioMicMute, exec, swayosd-client --input-volume mute-toggle

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# Menu de contrôle média (rofi centré, Esc pour fermer)
bind = ALT, P, exec, ~/.config/hypr/scripts/player-menu.sh

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hypr.land/Configuring/Window-Rules/ for more
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.
windowrule = match:class .*, suppress_event maximize

# Fix some dragging issues with XWayland
windowrule = match:class ^$, match:title ^$, match:xwayland true, match:float true, match:fullscreen false, match:pin false, no_focus on
