[General]
# Password mask character
passwordCharacter=*
# Mask password characters or not ("true" or "false")
passwordMask=true
# value "1" is all display width, "0.5" is a half of display width etc.
passwordInputWidth=0.5
# Background color of password input
passwordInputBackground=
# Radius of password input corners
passwordInputRadius=
# "true" for visible cursor, "false"
passwordInputCursorVisible=false
# Font size of password (in points)
passwordFontSize=96
passwordCursorColor=random
passwordTextColor=

# Show or not sessions choose label
showSessionsByDefault=false
# Font size of sessions choose label (in points).
sessionsFontSize=24

# Show or not users choose label
showUsersByDefault=false
# Font size of users choose label (in points)
usersFontSize=48

# Path to background (image or video). mp4 is supported by the QML above.
background=/usr/share/backgrounds/white1M.mp4

# Or use just one color
# backgroundFill=#000000

# Fill mode for background
# aspect = crop to fill while preserving aspect (default)
# fill   = stretch to fill (used for video too)
# tile   = only for still images
# pad    = only for still images
backgroundFillMode=aspect

# Default text color for all labels
basicTextColor=#000000

# Radius of background blur (set to 0 if video performance is poor)
blurRadius=

